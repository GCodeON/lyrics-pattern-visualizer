const spotify    = require('./spotify');
const genius     = require('./genius');
const musixmatch = require('./musixmatch');
const songs      = require('./songs');

module.exports = {
    spotify, genius, musixmatch, songs
}